<%- include('./partials/head') %> 
  <body>

    <%- include('./partials/navbar') %> 

    <div class="container">

        <% avisor.forEach(aviso => { %> 

        <div class="card shadow mt-4">

          <div class="card-header bg-roxo text-white pl-5 p-4 d-flex align-items-center justify-content-between">
            <h2 class="card-title"><i class="fas fa-exclamation-circle"></i> <%= aviso.titulo %> </h2>
            <h3 class="lead"><%= moment(aviso.data).format("DD/MM/YYYY") %></h3>
          </div>

          <div class="card-body p-5">
              <div class="d-flex align-items-center">
                <img src="img/perf.jpeg" class="img-thumbnail rounded-circle" width="75">
                    <div class="ml-2">
                        <div class="font-weight-bold">Téo B. Sitta</div>
                        <div class="text-secondary"> <%= moment(aviso.data,"YYYYMMDD").fromNow(); %>  </div>
                    </div>

              </div>

              <div class="mt-4">
                <%= aviso.Mensagem %>

              </div>
          </div>

        </div>

        <% }); %> 

    </div>
      
    <%- include('./partials/script_i') %> 
  </body>
</html>